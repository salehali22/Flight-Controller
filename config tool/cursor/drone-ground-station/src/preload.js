// Preload script - reserved for future IPC bridge (serial connection, etc.)
// Currently unused but kept minimal to satisfy Electron configuration.

window.addEventListener('DOMContentLoaded', () => {
  // Placeholder for future context-bridge APIs.
});

